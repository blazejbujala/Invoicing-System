server:
  port: 7777

spring:
  profiles:
    active: jpa
  flyway:
    enabled: true
  security:
    user:
      name: bb
      password: Pass123

logging:
  level:
    root: INFO
    org.springframework.boot: WARN
    pl.futurecollars.invoicing: INFO
    pl.futurecollars.invoicing.db: DEBUG

springdoc:
  swagger-ui:
    enabled: true

invoicing-system:
  csrf:
    disable: true